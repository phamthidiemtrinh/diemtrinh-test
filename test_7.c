//7 Nhập vào 1 số kiểm tra nó có phải số lẻ hay không
#include <stdio.h>
void main()
{
    int n;
    scanf("%d",&n);
    if(n%2!=0) printf ("so le");
        else printf("khong le");
}
